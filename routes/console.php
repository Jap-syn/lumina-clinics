<?php

/*
 | Deliberately empty.
 |
 | Lumina's shared hosting cannot run scheduled jobs, so this system has no
 | scheduled commands at all. Anything that would normally be a cron job is
 | handled lazily on the request path instead - see HoldSweeper.
 */
